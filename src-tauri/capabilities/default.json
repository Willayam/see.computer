{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pill",
  "description": "The pill listens for the `pill` event and emits back the size the stretched card needs and which of its two buttons was pressed. No commands and no invoke surface, but those two events do let the webview resize the window, take it out of click-through, and rewrite the clipboard.",
  "windows": ["pill"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten", "core:event:allow-emit"]
}
