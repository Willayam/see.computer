{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pill",
  "description": "The pill listens for the `pill` event and emits back the size the stretched card needs, plus which of its two buttons was pressed. No commands, no invoke surface.",
  "windows": ["pill"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten", "core:event:allow-emit"]
}
